#pragma once

namespace blackjack {

/// Difficulty affects shoe size and blackjack payout rules (see README class diagram).
enum class Difficulty {
    EASY,
    NORMAL,
    HARD,
};

} // namespace blackjack
